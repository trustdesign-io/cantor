# starter-local has no required environment variables.
#
# This file exists to satisfy the starter spec but is intentionally empty.
# Local-only projects run entirely from `npm run dev` with no hosted services,
# no authentication, and no deployment targets. Any future env vars specific
# to a forked project should be added here and documented in docs/SETUP.md.
