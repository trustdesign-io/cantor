/** Live tab — candlestick chart, EMA overlays, RSI panel, signal log.
 *  Implemented in ticket #10 (price chart), #11 (RSI panel), #12 (signal log), #13 (wire). */
export function LiveTab() {
  return (
    <div className="flex items-center justify-center h-full" style={{ color: 'var(--text-secondary)' }}>
      <span className="text-sm">Live — chart and signal log (coming in Phase 3)</span>
    </div>
  )
}
