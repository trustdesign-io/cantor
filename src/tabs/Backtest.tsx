/** Backtest tab — date range picker, run button, results.
 *  Implemented in ticket #17 (REST fetch), #18 (runner), #19 (UI). */
export function BacktestTab() {
  return (
    <div className="flex items-center justify-center h-full" style={{ color: 'var(--text-secondary)' }}>
      <span className="text-sm">Backtest — date picker and results (coming in Phase 5)</span>
    </div>
  )
}
